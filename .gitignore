# Rust target directory
/target/

# Cargo lockfile (uncomment if library, but binary crates usually commit Cargo.lock)
# Cargo.lock

# Unix Domain Socket & temporary files
*.sock
/tmp/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
